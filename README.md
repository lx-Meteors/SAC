# This is the Lingua-2 branch. 

- You need to reprocess the data using instruction_prepare_data.py. The parameters for Lingua-2 are set within this file.

- Then, start evaluating the dataset processed by Lingua-2 by using instruction_evaluator.py.